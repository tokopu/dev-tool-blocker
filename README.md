No longer being updated, still can be used.

Add the following script tag inside your `<head>` or `<body>`:

```html
<script src="https://unpkg.com/dev-tool-blocker@latest/dist/codesec.min.js"></script>
